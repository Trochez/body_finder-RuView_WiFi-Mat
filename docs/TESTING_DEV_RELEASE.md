# Deprecated protocol-v1 test instructions

The manual-coordinate protocol-v1 procedure is obsolete.

For protocol v2 and all `0.2.0-experimental.2` / later automatic-geometry prereleases, use:

**`docs/TESTING_AUTOGEOMETRY_RELEASE.md`**

Do not enter sensor X/Y/Z into the application or native node. Physical coordinates are external ground truth only and must never be fed into the production automatic-geometry solver.
