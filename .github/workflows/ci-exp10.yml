name: CI experimental.10 (historical)

on:
  workflow_dispatch:

jobs:
  historical:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'experimental.10 CI is historical; active matrix is ci-exp11.yml'
